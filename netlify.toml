# Netlify Configuration for VedaAI Next.js Frontend
[build]
  base = ""
  publish = "apps/frontend/.next"
  command = "npm run build -w @vedaai/shared-types && npm run build -w @vedaai/frontend"

[build.environment]
  NODE_VERSION = "20"
